syntax = "proto3";

package users;


service Users {
  rpc Create(User) returns (User) {};
  rpc List(RequestType) returns (stream User) {};
}

message RequestType {
}

message User {
  int64 id = 1;
  string name = 2;
}
