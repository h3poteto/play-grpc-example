syntax = "proto3";

package users;


service Users {
  rpc Create(CwUserId) returns (CwUserId) {};
  rpc List(RequestType) returns (stream CwUserId) {};
}

message RequestType {
}

message CwUserId {
  int64 id = 1;
}
